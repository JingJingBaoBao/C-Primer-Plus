9.1
头文件中包括：
函数原型
define或const定义的符号常量
结构声明
类声明
模板声明
内联函数

9.2
自动存储持续性：函数内创建的变量在执行到代码块时被创建，执行完代码块自动释放
静态存储持续性：函数外定义的变量或static定义的变量为静态，在程序整个运行过程都存在
动态存储持续性：用new分配的内存将一直存在，直到用delete将其释放或程序结束为止

变量前面加::表示访问全局变量

extern从另一个文件中引入变量
static将变量作用于限制在本文件内


